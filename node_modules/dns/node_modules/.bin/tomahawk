#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../tomahawk/bin/tomahawk" "$@"
  ret=$?
else 
  node  "$basedir/../tomahawk/bin/tomahawk" "$@"
  ret=$?
fi
exit $ret
