#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../dns/bin/dns" "$@"
  ret=$?
else 
  node  "$basedir/../dns/bin/dns" "$@"
  ret=$?
fi
exit $ret
